# Backend API (used for REST + WebSocket). No trailing slash.
# Local: default in code is http://localhost:8000 if unset.
VITE_API_BASE=http://localhost:8000

# Optional: override WebSocket origin (default: derived from VITE_API_BASE: http->ws, https->wss)
# VITE_WS_BASE=ws://localhost:8000
